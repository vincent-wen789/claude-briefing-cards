## 汇报卡 briefing-card（自动触发）
你要给我做①决策请求/让我选 ②多个项目的进度汇报 ③一批待我确认/拍板的清单 时 → **默认动作是过 T0–T3 路由闸**（不是默认甩文字），当场判该用哪档：
- **T0** 一句话能说清 / 单一事实 / 闲聊 → 纯文字（**绝不画卡**）
- **T1** 2-4 个选项、每项利弊一行讲得清、不需链接/图 → AskUserQuestion
- **T2** 决策需多维对比/证据链接 · 进度 ≥3 线或含 blocker · 确认 ≥3 项带出处 → **load briefing-card skill 画聊天内卡片**
- **T3** 要存档/发别人/带内容图/超两屏 → HTML 文件（转 huashu-design）

**翻转默认（2026-06-18 · 手动召唤太累）：汇报/进度/决策/选项/确认/拍板 默认就出卡**——举证责任反过来，不判「够不够 T2」而判「能不能安全降到 T0/T1」：只有一句话能说清/单一事实/闲聊降 T0，2-4 选项压得进一行降 T1，否则一律出卡，别犹豫。防卡墙节流（1 小时 ≥2 卡自动降文字）仍生效，刷屏风险它兜底，所以放心偏出卡。判到 T2/T3 才 load skill 看模板（三类卡模板 + 接收端守则 + 降级规则）；收到 `决策卡「/进度卡「/确认卡「` 开头的回传也 load（先过接收端守则）。

**我手动召唤**（我自己想要 briefing 时）：`做个 briefing` / `brief 一下 X` / `给我个概览` / `汇总成卡` / `出个卡` / `画个决策卡·进度卡·确认卡` → 我开口要就给，类型没指定你按内容判，**门槛放宽**（默认出卡，只有内容撑不满卡才回文字）；手动召唤**无视本 session「别画了」降级**。我用意思相近但非正式词的说法（整理一下/捋一捋/做个总结）你也照办，每 session 至多轻提一次正式召唤词。

**不触发**：本 session 我说过「别画了」/ 已明确要纯文字（仅挡自动触发，不挡上面手动召唤）。
